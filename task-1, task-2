Task-1

y = [[2, 3, 5, 1], [0, 2], [1]]

list_max = max(y, key=lambda x: len(x))
list_len = len(max_list)
print(list_max, list_len)



Task-2

def divisible(x,y):
    if y == 0 or x == 0:
        return "error"
    elif y % x == 0:
            return "divisible"
    else:
        return "not divisible"
